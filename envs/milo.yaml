name: milo_env

channels:
  - conda-forge
  - defaults

dependencies:
  # === Core Dependencies from pyproject.toml ===
  - python=3.9
  - joblib=1.2
  - numpy=1.23
  - pandas=2.0
  - matplotlib>=3.7.5
  - seaborn=0.13
  - scipy=1.11
  - scikit-learn=1.3

  # === Pip installation ===
  - pip:
    # This points to your source code
    - -e /data/rds/DMP/UCEC/GENEVOD/qguo/Software/milo_package
